spring:
  application:
    name: microservice-microservice-dialog

  cloud:
    kubernetes:
      secrets:
        sources:
          - name: microservice-microservice-dialog
      config:
        sources:
          - name: library-core
          - name: microservice-microservice-dialog
      reload:
        enabled: true

management:
  endpoint:
    restart:
      enabled: true
    health:
      enabled: true
    info:
      enabled: true
    prometheus:
      enabled: true
